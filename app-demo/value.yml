replicaCount: 1

image:
  repository: lasmor2025/demo-app
  tag: v2
  pullPolicy: IfNotPresent

service:
  type: LoadBalancer
  port: 80
  targetPort: 3000

ingress:
  enabled: true
  host: demo.local
  className: nginx
